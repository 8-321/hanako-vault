<!-- experience-title: dXNlci1wcmVmZXJlbmNl -->
1. kel 的模型 provider 偏好：默认用 `an` provider（base_url 为 an520.xin，api 为 openai-completions），`openai` provider 因历史原因曾被配成 openai-codex-responses 会走官方 API 出 401。配置类动作不用反复确认走哪个 provider，直接默认 `an`。
