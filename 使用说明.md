# 黑曜石Vault 使用说明

## 最简单用法

双击：

```text
D:\小柯 Ke\黑曜石Vault\一键备份黑曜石记录.bat
```

它会自动把三个 Obsidian 库的正文和附件备份到本库。

批处理入口内部只调用 `scripts` 英文目录，避免 Windows `cmd` 读取中文路径时乱码。

## 自动备份

双击：

```text
D:\小柯 Ke\黑曜石Vault\一键安装自动备份.bat
```

安装后，系统会每 15 分钟自动运行一次：

1. 备份三个 Obsidian 库到 `黑曜石Vault`
2. 更新 `备份索引/总索引.md`
3. 有变化就自动提交到本地 Git
4. 尝试推送到 GitHub

如果 GitHub 临时失败，本地备份和本地提交仍然保留。

状态页：

```text
D:\小柯 Ke\黑曜石Vault\备份索引\自动备份状态.md
```

它会告诉你最近一次自动备份是否正常，以及 GitHub 推送有没有失败。这个状态页是本机状态，不进入 GitHub 提交；这样不会因为每 15 分钟更新时间就刷一堆无意义提交。

## 怎么找东西

先打开：

```text
D:\小柯 Ke\黑曜石Vault\备份索引\总索引.md
```

再按来源或最近修改时间找。

## 日记三端写入

Codex 写日记时，不再把重复出错的对话记录当成正文记录反复更新。日记统一用脚本追加到三个库：

```text
D:\小柯 Ke\黑曜石Vault\scripts\write-three-vault-diary.ps1
```

当前写入位置：

- `D:\小柯 Ke\HanakoVault\日记`
- `D:\小柯 Ke\小柯自媒体Vault\00 - 每日笔记`
- `D:\小柯 Ke\小柯日常Vault\00 - 每日笔记`

脚本会给每条 Codex 日记加一个隐藏标记；同一标记已经存在时会跳过，避免同一句日记被反复追加。

## Codex 对话沉淀系统

现在不是简单生成两张卡，而是一套五层流程：

1. Capture：对话备份卡。
2. Distill：Skill 候选卡。
3. Index：对话索引和 Skill 候选索引。
4. Review：候选先留在黑曜石里检查。
5. Promote：Kel 明确要求后再转成正式 `SKILL.md`。

系统说明：

```text
D:\小柯 Ke\黑曜石Vault\99 - Agent指南与规范\Codex对话沉淀系统.md
```

每次 Codex 对话结束前，运行固定入口：

```text
D:\小柯 Ke\黑曜石Vault\scripts\complete-codex-conversation.ps1
```

它会做三件事：

1. 在 `收件箱\Codex对话备份\<YYYY-MM>` 生成一份对话备份卡。
2. 在 `技能\自动生成\候选\<YYYY-MM>` 生成一份 Skill 候选卡。
3. 更新 `备份索引\Codex对话沉淀索引.md` 和 `技能\Skill候选索引.md`。
4. 调用黑曜石总备份脚本，刷新三个工作库的总备份与索引。

这不是把原始聊天全文重复塞进黑曜石，而是把“这次对话有什么可复用经验”沉淀成可检查、可转正的 Skill 候选。

候选 Skill 要转成正式技能时，先预览：

```text
D:\小柯 Ke\黑曜石Vault\scripts\promote-skill-candidate.ps1 -DryRun
```

确认目标后再转正，避免一次性经验污染正式技能库。

## 为什么不直接把所有库混在一起

三个库承担的角色不同：

- HanakoVault 偏 AI 和 Agent。
- 小柯自媒体Vault 偏内容生产。
- 小柯日常Vault 偏生活学习。

混在一起会让查找和同步更乱。本库只做总备份和总入口。

## GitHub 说明

本库可以作为 GitHub 备份仓库。GitHub 负责保存历史版本，不负责实时同步。

GitHub 不支持用账号密码直接推送，需要浏览器授权、token 或 SSH。

明显的密钥、密码、token、凭据文件不会进入这个总备份库。
